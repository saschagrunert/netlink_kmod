#include "sockets.h"

/// Initialize the socket
int socket_initialize(netlink_device_t * device)
{
    int ret = 0;
    return ret;
}

/// Uninitialize the socket
void socket_destroy(netlink_device_t * device)
{
}
